{
  "name": "oasis",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "reinstall": "yarn clean && yarn install",
    "prepare": "husky install",
    "commit": "git add . && cz && git push",
    "deploy": "sh bin/deploy.sh",
    "deploy:prod": "git add . && git commit -m \"chore(deploy): Merge staging -> prod\" &&git pull origin staging  && git checkout prod && git merge staging && git push origin prod && git checkout staging",
    "lint": "yarn workspaces foreach --exclude @oasis/playwright,@oasis/client-gql -p run lint && yarn workspaces foreach --exclude @oasis/playwright,@oasis/client-gql -p run prettify",
    "dev:web": "yarn workspace @oasis/web dev",
    "dev:api": "yarn workspace @oasis/api dev",
    "dev:gql": "yarn workspace @oasis/client-gql dev",
    "build:gql": "yarn workspace @oasis/api run build && yarn workspace @oasis/client-gql run build",
    "build": "yarn workspace @oasis/api run build && yarn workspace @oasis/client-gql run build && yarn workspace @oasis/web run build",
    "dev": "yarn workspace @oasis/web dev",
    "start": "yarn workspace @oasis/web run start",
    "clean:all": "yarn workspace @oasis/api run clean && yarn workspace @oasis/client-gql run clean"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.2",
    "@commitlint/config-conventional": "^12.1.2",
    "@types/eslint": "^7.2.10",
    "@types/jest": "^26.0.23",
    "@types/rimraf": "^3",
    "commitizen": "^4.2.3",
    "cz-conventional-changelog": "3.3.0",
    "husky": "^6.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.6"
  },
  "dependencies": {
    "typeorm": "^0.2.32",
    "typescript": "^4.2.4"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog",
      "defaultScope": "global"
    }
  }
}
