{
  "name": "@oasis-sh/parser",
  "private": true,
  "version": "0.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc && copyfiles -u 1 markdown/**styles/* dist/markdown",
    "clean": "rimraf dist/"
  },
  "dependencies": {
    "typescript": "^4.3.4"
  },
  "devDependencies": {
    "@types/copyfiles": "^2.4.0",
    "@types/rimraf": "^3.0.0",
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2"
  }
}
